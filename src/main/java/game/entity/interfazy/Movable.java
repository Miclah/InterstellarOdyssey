package game.entity.interfazy;

/**
 * The interface Movable.
 */
public interface Movable {
    /**
     * Makes an npc move by moving its coordinates on the world map,
     * also changes its images to create a walking animation
     */
    void move();
}
